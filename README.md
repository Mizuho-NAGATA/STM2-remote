## 🔧 接続できないときの確認ポイント（よくある原因）
- STM‑2接続PCの **IPアドレスが想定と異なる**（DHCP予約の変更、ネットワーク機器更新、リース更新など）  
  - まず STM‑2接続PC で現在のIPを確認してください（例：Windows `ipconfig` / macOS・Linux `ifconfig` または `ip a`）
- ファイアウォールがブロックしている（ポート3000 / 8086 など）
- **Windows のネットワークプロファイルが「パブリック」**になっており、通信が遮断されている

